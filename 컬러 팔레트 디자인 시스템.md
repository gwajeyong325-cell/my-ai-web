# 컬러 팔레트 디자인 시스템

## 프로젝트 정보

- **출처 웹사이트**: What If — 홍익대학교 산업디자인 졸업작품전 (whatif-hongik.com)
- **분석 날짜**: 2026-06-08
- **적용 프로젝트**: 포트폴리오 웹사이트
- **디자인 컨셉**: Dark Luxury — 우주적이고 몰입감 있는 다크 럭셔리 톤

---

## CSS 변수 정의

```css
:root {
  /* Primary Colors — 로열 블루 계열 */
  --color-primary:       #1A3FA0;
  --color-primary-light: #2D5BC9;
  --color-primary-dark:  #0D1A5C;

  /* Secondary Colors — 딥 네이비 계열 */
  --color-secondary:     #0A0E2E;
  --color-secondary-deep:#050818;

  /* Accent Colors — 오렌지 포인트 */
  --color-accent:        #E8520A;
  --color-accent-light:  #F4A24D;
  --color-accent-dark:   #C43E06;

  /* Background Colors */
  --color-bg-primary:    #050818;
  --color-bg-secondary:  #0A0E2E;
  --color-bg-surface:    #0D1A5C;
  --color-bg-elevated:   #1A2472;

  /* Text Colors */
  --color-text-primary:  #FFFFFF;
  --color-text-secondary:#B0B8CC;
  --color-text-muted:    #6B7899;

  /* Border / Outline */
  --color-border:        rgba(255, 255, 255, 0.10);
  --color-border-strong: rgba(255, 255, 255, 0.25);
  --color-border-subtle: rgba(255, 255, 255, 0.05);

  /* Interactive Colors — 버튼·링크 */
  --color-button-primary: #E8520A;
  --color-button-hover:   #F4A24D;
  --color-button-outline: rgba(255, 255, 255, 0.15);
  --color-link:           #FFFFFF;
  --color-link-hover:     #F4A24D;

  /* Gradient Presets */
  --gradient-hero:   linear-gradient(135deg, #050818 0%, #0A0E2E 50%, #1A3FA0 100%);
  --gradient-accent: linear-gradient(90deg, #E8520A 0%, #F4A24D 100%);
  --gradient-surface:linear-gradient(180deg, #0D1A5C 0%, #050818 100%);

  /* Shadow */
  --shadow-glow-blue:   0 0 40px rgba(26, 63, 160, 0.4);
  --shadow-glow-orange: 0 0 20px rgba(232, 82, 10, 0.5);
  --shadow-card:        0 8px 32px rgba(0, 0, 0, 0.6);
}
```

---

## 컬러 사용 가이드

### Background — 배경색

| 변수 | Hex | 사용 상황 |
|---|---|---|
| `--color-bg-primary` | `#050818` | 페이지 최외각 배경, 섹션 구분 끝단 |
| `--color-bg-secondary` | `#0A0E2E` | 메인 콘텐츠 영역 배경 |
| `--color-bg-surface` | `#0D1A5C` | 카드, 모달, 드롭다운 등 부유하는 요소 |
| `--color-bg-elevated` | `#1A2472` | 호버 상태의 카드, 선택된 메뉴 항목 |

```css
/* 예시: 페이지 전체 배경 */
body {
  background-color: var(--color-bg-primary);
}

/* 예시: 카드 컴포넌트 */
.card {
  background-color: var(--color-bg-surface);
  border: 1px solid var(--color-border);
}
```

---

### Primary — 블루 계열

**로열 블루**는 신뢰감과 창의성을 동시에 전달하는 핵심 브랜드 색상입니다.

| 변수 | Hex | 사용 상황 |
|---|---|---|
| `--color-primary` | `#1A3FA0` | 주요 버튼 배경, 섹션 구분선, 활성 상태 |
| `--color-primary-light` | `#2D5BC9` | hover 상태, 강조 배경 |
| `--color-primary-dark` | `#0D1A5C` | active(클릭) 상태, 그림자 영역 |

```css
/* 예시: 주요 버튼 */
.btn-primary {
  background-color: var(--color-primary);
  color: var(--color-text-primary);
  transition: background-color 0.2s ease;
}
.btn-primary:hover {
  background-color: var(--color-primary-light);
}
.btn-primary:active {
  background-color: var(--color-primary-dark);
}
```

---

### Accent — 오렌지 계열

**번트 오렌지**는 전체 면적의 약 3%만 사용하되, 시선을 즉시 끌어당기는 앵커 포인트 역할을 합니다.  
**절대로 과도하게 사용하지 마세요** — 희소성이 강조 효과를 만듭니다.

| 변수 | Hex | 사용 상황 |
|---|---|---|
| `--color-accent` | `#E8520A` | CTA 버튼, 뱃지, 핵심 강조 텍스트 |
| `--color-accent-light` | `#F4A24D` | hover 상태, 그라디언트 끝점 |
| `--color-accent-dark` | `#C43E06` | active 상태, 진한 강조 |

```css
/* 예시: CTA 버튼 */
.btn-cta {
  background: var(--gradient-accent);
  color: var(--color-text-primary);
  box-shadow: var(--shadow-glow-orange);
}

/* 예시: 강조 텍스트 */
.text-highlight {
  color: var(--color-accent);
}
```

---

### Text — 텍스트 색상

배경이 어둡기 때문에 텍스트는 **화이트 계열**만 사용합니다.  
명도 차이로 정보 위계(Hierarchy)를 구성합니다.

| 변수 | Hex | 사용 상황 |
|---|---|---|
| `--color-text-primary` | `#FFFFFF` | 헤딩, 주요 본문, 메뉴 텍스트 |
| `--color-text-secondary` | `#B0B8CC` | 서브 텍스트, 설명글, 날짜·태그 |
| `--color-text-muted` | `#6B7899` | 비활성 메뉴, placeholder, 주석 |

```css
/* 예시: 정보 위계 */
h1, h2 { color: var(--color-text-primary); }
p       { color: var(--color-text-secondary); }
small   { color: var(--color-text-muted); }
```

---

### Interactive — 상호작용 요소

| 변수 | Hex | 사용 상황 |
|---|---|---|
| `--color-button-primary` | `#E8520A` | 주요 CTA 버튼 |
| `--color-button-hover` | `#F4A24D` | 버튼 hover 상태 |
| `--color-button-outline` | `rgba(255,255,255,0.15)` | 아웃라인 버튼 배경 |
| `--color-link` | `#FFFFFF` | 기본 링크 색상 |
| `--color-link-hover` | `#F4A24D` | 링크 hover 상태 |

```css
/* 예시: 아웃라인 버튼 */
.btn-outline {
  background: var(--color-button-outline);
  border: 1px solid var(--color-border-strong);
  color: var(--color-text-primary);
  backdrop-filter: blur(8px);
}
.btn-outline:hover {
  background: var(--color-primary);
}

/* 예시: 네비게이션 링크 */
nav a {
  color: var(--color-link);
  text-decoration: none;
}
nav a:hover {
  color: var(--color-link-hover);
}
```

---

## 그라디언트 사용 가이드

```css
/* 히어로 섹션 배경 */
.hero {
  background: var(--gradient-hero);
  /* 또는 메시 그라디언트: */
  background:
    radial-gradient(ellipse at 70% 60%, rgba(26, 63, 160, 0.6) 0%, transparent 60%),
    radial-gradient(ellipse at 30% 40%, rgba(232, 82, 10, 0.3) 0%, transparent 50%),
    #050818;
}

/* 섹션 전환 페이드 */
.section-fade {
  background: var(--gradient-surface);
}

/* 강조 배지 */
.badge-accent {
  background: var(--gradient-accent);
  padding: 4px 12px;
  border-radius: 999px;
}
```

---

## 컬러 사용 비율 원칙

```
배경 계열 (딥 네이비·블랙)  ████████████████████████████  70%
블루 계열 (Primary)         █████████████                 18%
화이트 (텍스트)              ████                           8%
오렌지 (Accent)             █                              3%
그레이 (보조 텍스트)         ▌                              1%
```

> **핵심 원칙**: 오렌지는 아껴 쓸수록 강해집니다.  
> 전체 디자인에서 오렌지가 보이는 요소가 2~3개를 넘지 않도록 하세요.

---

## 반응형 고려사항

### 다크모드 (기본 모드)

이 팔레트 자체가 다크 테마 기반이므로, 별도의 `prefers-color-scheme: dark` 처리 없이도 다크모드에 최적화되어 있습니다.

### 라이트모드 대응 (선택 사항)

```css
@media (prefers-color-scheme: light) {
  :root {
    --color-bg-primary:    #F0F4FF;
    --color-bg-secondary:  #E8EDF8;
    --color-bg-surface:    #FFFFFF;

    --color-text-primary:  #0A0E2E;
    --color-text-secondary:#2D3A6B;
    --color-text-muted:    #6B7899;

    --color-border:        rgba(10, 14, 46, 0.12);
    --color-border-strong: rgba(10, 14, 46, 0.25);

    /* Primary·Accent는 동일하게 유지 */
  }
}
```

### 모바일 고려사항

```css
/* 모바일에서는 글로우 효과 비활성화 (성능 최적화) */
@media (max-width: 768px) {
  .card {
    box-shadow: var(--shadow-card);  /* 글로우 대신 단순 그림자 */
  }
  .btn-cta {
    box-shadow: none;
  }
}

/* 터치 디바이스에서 hover 대신 active 상태 강조 */
@media (hover: none) {
  .btn-primary:active {
    background-color: var(--color-primary-light);
    transform: scale(0.98);
  }
}
```

### 고대비 모드 접근성 대응

```css
@media (forced-colors: active) {
  :root {
    --color-text-primary:  ButtonText;
    --color-bg-primary:    Canvas;
    --color-button-primary:ButtonFace;
    --color-link:          LinkText;
  }
}
```

---

## MUI 테마 적용 예시 (React + MUI v9)

```js
// src/theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    mode: 'dark',
    primary: {
      main:  '#1A3FA0',
      light: '#2D5BC9',
      dark:  '#0D1A5C',
    },
    secondary: {
      main:  '#E8520A',
      light: '#F4A24D',
      dark:  '#C43E06',
    },
    background: {
      default: '#050818',
      paper:   '#0A0E2E',
    },
    text: {
      primary:   '#FFFFFF',
      secondary: '#B0B8CC',
      disabled:  '#6B7899',
    },
    divider: 'rgba(255, 255, 255, 0.10)',
  },
  components: {
    MuiButton: {
      styleOverrides: {
        containedPrimary: {
          background: 'linear-gradient(90deg, #E8520A 0%, #F4A24D 100%)',
          '&:hover': {
            background: 'linear-gradient(90deg, #C43E06 0%, #E8520A 100%)',
          },
        },
      },
    },
    MuiCard: {
      styleOverrides: {
        root: {
          backgroundColor: '#0D1A5C',
          border: '1px solid rgba(255, 255, 255, 0.10)',
        },
      },
    },
  },
});

export default theme;
```

---

## 색상 접근성 체크 (WCAG 2.1)

| 조합 | 대비율 | 등급 | 판정 |
|---|---|---|---|
| `#FFFFFF` 텍스트 / `#050818` 배경 | 19.8 : 1 | AAA | ✅ 통과 |
| `#FFFFFF` 텍스트 / `#0A0E2E` 배경 | 17.2 : 1 | AAA | ✅ 통과 |
| `#B0B8CC` 텍스트 / `#050818` 배경 | 10.4 : 1 | AAA | ✅ 통과 |
| `#FFFFFF` 텍스트 / `#E8520A` 버튼 |  3.1 : 1 | AA (대형) | ⚠️ 큰 텍스트만 허용 |
| `#6B7899` 텍스트 / `#050818` 배경 |  5.2 : 1 | AA  | ✅ 통과 |

> **주의**: 오렌지(`#E8520A`) 위에 흰 텍스트는 소형(14px 이하)에는 사용하지 마세요.
